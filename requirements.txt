# Python 3.12 is required: facenet-pytorch pins torch<2.3, which has no
# wheels for 3.13.
#
# Install in three steps. Each extra step exists for a stated reason.
#
# 1. CPU-only torch, or pip may fetch a ~2.5 GB CUDA build nothing here uses:
#      pip install torch==2.2.2 torchvision==0.17.2 --index-url https://download.pytorch.org/whl/cpu
# 2. This file:
#      pip install -r requirements.txt
# 3. The in-process EVM, see requirements-evm.txt:
#      pip install --no-deps -r requirements-evm.txt

# Face detection and embedding. facenet-pytorch pins the four versions below
# exactly; they are repeated here so the resolver settles in one pass instead of
# backtracking through a 200 MB download.
facenet-pytorch==2.6.0
torch==2.2.2
torchvision==0.17.2
numpy==1.26.4
Pillow==10.2.0

# Search stage
requests>=2.31.0

# Blockchain. Pinned below web3 8 while its API settles.
web3>=7.0.0,<8.0.0
py-solc-x>=2.0.2

# Dependencies of py-evm, which requirements-evm.txt installs with --no-deps.
eth-bloom>=1.0.3
cached-property>=1.5.1
lru-dict>=1.1.6
trie>=2.0.0
py-ecc>=5.2.0
semantic-version>=2.6.0

# CLI and config
typer>=0.12.0
python-dotenv>=1.0.1

# Tests
pytest>=8.0.0
