[pytest]
testpaths = tests
markers =
    model: needs the pretrained weights, which are downloaded on first use.
        Deselected by default so the suite stays offline; run with -m model.
addopts = -m "not model" -q
